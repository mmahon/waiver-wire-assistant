{
  "name": "waiver-wire-assistant",
  "version": "0.1.0",
  "description": "Recommends waiver add/drop moves for an ESPN fantasy football team, with bye-week lookahead and claim-or-wait advice.",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "mock": "node server.js --mock",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": { "node": ">=20.6" }
}
